---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Pintura - Calidad Premium">
  <!-- Hero Section -->
 <section class="bg-[#145ef9] py-20 text-center text-white rounded-xl">
    <div class="text-center">
     
      </div>
      <h1 class="text-5xl font-bold mb-4">Customización de vehiculos</h1>
    </div>
      <p class="text-xl opacity-90 max-w-2xl mx-auto">
        Dale forma y estilo a tus vehiculos con nuestra amplia gama de diseños
      </p>
    </div>
  </section>

  <!-- Carrusel Principal -->
  <section class="mb-16">
    <h2 class="text-3xl font-bold text-center mb-8 mt-10 text-gray-800">
  Nuestros Productos Destacados
</h2>

    
    <div class="relative max-w-6xl mx-auto">
      <!-- Carrusel Container -->
      <div class="carousel-container relative overflow-hidden rounded-xl shadow-lg">
        <div class="carousel-track flex transition-transform duration-500 ease-in-out" id="carouselTrack">
          
          <!-- Slide 1 -->
          <div class="carousel-slide min-w-full bg-white p-8 flex items-center">
            <div class="w-1/2 pr-8">
              <h3 class="text-2xl font-bold mb-4 text-gray-800">Pintura Látex Interior</h3>
              <p class="text-gray-600 mb-4">
                Pintura de alta cobertura para interiores, con acabado mate que disimula imperfecciones 
                y proporciona un ambiente acogedor.
              </p>
              <ul class="text-gray-600 mb-6 space-y-2">
                <li class="flex items-center"><span class="text-green-500 mr-2">✓</span>Fácil aplicación</li>
                <li class="flex items-center"><span class="text-green-500 mr-2">✓</span>Secado rápido</li>
                <li class="flex items-center"><span class="text-green-500 mr-2">✓</span>Lavable</li>
              </ul>
              <a href="/?scrollTo=contacto" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors inline-block text-center">
                Ver Detalles
              </a>
            </div>
            <div class="w-1/2 flex justify-center">
              <div class="w-80 h-64 rounded-lg overflow-hidden shadow-md">
                <img 
                  src="/imagen1.avif" 
                  alt="Pintura Látex Interior Pintuco" 
                  class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                />
              </div>
            </div>
          </div>

          <!-- Slide 2 -->
          <div class="carousel-slide min-w-full bg-white p-8 flex items-center">
            <div class="w-1/2 pr-8">
              <h3 class="text-2xl font-bold mb-4 text-gray-800">Esmalte Sintético</h3>
              <p class="text-gray-600 mb-4">
                Esmalte de alta resistencia para maderas y metales, con acabado brillante 
                que protege contra la humedad y el desgaste.
              </p>
              <ul class="text-gray-600 mb-6 space-y-2">
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Resistente al agua</li>
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Acabado brillante</li>
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Protección UV</li>
              </ul>
              <a href="/?scrollTo=contacto" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors inline-block text-center">
                Ver Detalles
              </a>
            </div>
            <div class="w-1/2 flex justify-center">
              <div class="w-80 h-64 bg-gradient-to-br from-blue-100 to-blue-300 rounded-lg flex items-center justify-center shadow-md">
                <div class="text-center">
                  <div class="w-24 h-24 bg-blue-600 rounded-full mx-auto mb-4 flex items-center justify-center">
                    <svg class="w-12 h-12 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                  </div>
                  <p class="font-semibold text-blue-800">Esmalte Sintético</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Slide 3 -->
          <div class="carousel-slide min-w-full bg-white p-8 flex items-center">
            <div class="w-1/2 pr-8">
              <h3 class="text-2xl font-bold mb-4 text-gray-800">Pintura Anticorrosiva</h3>
              <p class="text-gray-600 mb-4">
                Primer especializado para proteger superficies metálicas contra la oxidación, 
                ideal para estructuras expuestas al exterior.
              </p>
              <ul class="text-gray-600 mb-6 space-y-2">
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Protección total</li>
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Base para esmalte</li>
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Larga duración</li>
              </ul>
              <a href="/?scrollTo=contacto" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors inline-block text-center">
                Ver Detalles
              </a>
            </div>
            <div class="w-1/2 flex justify-center">
              <div class="w-80 h-64 bg-gradient-to-br from-blue-100 to-blue-300 rounded-lg flex items-center justify-center shadow-md">
                <div class="text-center">
                  <div class="w-24 h-24 bg-blue-600 rounded-full mx-auto mb-4 flex items-center justify-center">
                    <svg class="w-12 h-12 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z"/>
                    </svg>
                  </div>
                  <p class="font-semibold text-blue-800">Anticorrosiva</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Slide 4 -->
          <div class="carousel-slide min-w-full bg-white p-8 flex items-center">
            <div class="w-1/2 pr-8">
              <h3 class="text-2xl font-bold mb-4 text-gray-800">Pintura Exterior</h3>
              <p class="text-gray-600 mb-4">
                Pintura formulada especialmente para exteriores, resistente a los rayos UV 
                y condiciones climáticas extremas.
              </p>
              <ul class="text-gray-600 mb-6 space-y-2">
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Resistente al clima</li>
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>No se descolora</li>
                <li class="flex items-center"><span class="text-blue-500 mr-2">✓</span>Fácil mantenimiento</li>
              </ul>
              <a href="/?scrollTo=contacto" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors inline-block text-center">
                Ver Detalles
              </a>
            </div>
            <div class="w-1/2 flex justify-center">
              <div class="w-80 h-64 bg-gradient-to-br from-purple-100 to-blue-300 rounded-lg flex items-center justify-center shadow-md">
                <div class="text-center">
                  <div class="w-24 h-24 bg-blue-600 rounded-full mx-auto mb-4 flex items-center justify-center">
                    <svg class="w-12 h-12 text-white" fill="currentColor" viewBox="0 0 20 20">
                      <path d="M10.394 2.08a1 1 0 00-.788 0l-7 3a1 1 0 000 1.84L5.25 8.051a.999.999 0 01.356-.257l4-1.714a1 1 0 11.788 1.838L7.667 9.088l1.94.831a1 1 0 00.787 0l7-3a1 1 0 000-1.838l-7-3zM3.31 9.397L5 10.12v4.102a8.969 8.969 0 00-1.05-.174 1 1 0 01-.89-.89 11.115 11.115 0 01.25-3.762zM9.3 16.573A9.026 9.026 0 007 14.935v-3.957l1.818.78a3 3 0 002.364 0l5.508-2.361a11.026 11.026 0 01.25 3.762 1 1 0 01-.89.89 8.968 8.968 0 00-5.35 2.524 1 1 0 01-1.4 0zM6 18a1 1 0 001-1v-2.065a8.935 8.935 0 00-2-.712V17a1 1 0 001 1z"/>
                    </svg>
                  </div>
                  <p class="font-semibold text-blue-800">Exterior</p>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>

      <!-- Controles del Carrusel -->
      <button 
        class="carousel-btn carousel-prev absolute left-4 top-1/2 transform -translate-y-1/2 bg-white bg-opacity-90 hover:bg-opacity-100 text-gray-800 w-12 h-12 rounded-full shadow-lg flex items-center justify-center transition-all duration-300 hover:scale-110"
        onclick="prevSlide()"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
      </button>

      <button 
        class="carousel-btn carousel-next absolute right-4 top-1/2 transform -translate-y-1/2 bg-white bg-opacity-90 hover:bg-opacity-100 text-gray-800 w-12 h-12 rounded-full shadow-lg flex items-center justify-center transition-all duration-300 hover:scale-110"
        onclick="nextSlide()"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
      </button>

      <!-- Indicadores -->
      <div class="flex justify-center mt-6 space-x-2">
        <button class="carousel-indicator active w-3 h-3 rounded-full bg-blue-600 transition-all duration-300" onclick="goToSlide(0)"></button>
        <button class="carousel-indicator w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-all duration-300" onclick="goToSlide(1)"></button>
        <button class="carousel-indicator w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-all duration-300" onclick="goToSlide(2)"></button>
        <button class="carousel-indicator w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-all duration-300" onclick="goToSlide(3)"></button>
      </div>
    </div>
  </section>

  <!-- Sección de Información Adicional -->
  <section class="bg-gray-100 rounded-lg p-8 mb-12">
  <div class="grid md:grid-cols-3 gap-10">

    <!-- Calidad -->
    <div class="text-center bg-white rounded-2xl shadow-md p-6">
      <div class="w-16 h-16 rounded-full mx-auto mb-4 flex items-center justify-center bg-blue-100 shadow-sm">
        <svg class="w-7 h-7 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
          <path d="M9 12l2 2 4-4"/>
          <path d="M10 18a8 8 0 100-16 8 8 0 000 16z" fill="none" stroke="currentColor" stroke-width="2"/>
        </svg>
      </div>
      <h3 class="text-xl font-bold mb-2">Calidad Garantizada</h3>
      <p class="text-gray-600">Todas nuestras pinturas cumplen con los más altos estándares de calidad</p>
    </div>

    <!-- Gama -->
    <div class="text-center bg-white rounded-2xl shadow-md p-6">
      <div class="w-16 h-16 rounded-full mx-auto mb-4 flex items-center justify-center bg-green-100 shadow-sm">
        <svg class="w-7 h-7 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
          <path d="M3 4h14v4H3zM3 10h8v6H3zM14 10h3v6h-3z"/>
        </svg>
      </div>
      <h3 class="text-xl font-bold mb-2">Amplia Gama</h3>
      <p class="text-gray-600">Variedad de colores y acabados para todos tus proyectos</p>
    </div>

    <!-- Entrega -->
    <div class="text-center bg-white rounded-2xl shadow-md p-6">
      <div class="w-16 h-16 rounded-full mx-auto mb-4 flex items-center justify-center bg-orange-100 shadow-sm">
        <svg class="w-7 h-7 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
          <path d="M2 6l8 4 8-4v8a2 2 0 01-2 2H4a2 2 0 01-2-2V6z"/>
          <path d="M2 6l8 4 8-4" stroke="currentColor" stroke-width="2" fill="none"/>
        </svg>
      </div>
      <h3 class="text-xl font-bold mb-2">Entrega Rápida</h3>
      <p class="text-gray-600">Delivery en el menor tiempo posible a toda la ciudad</p>
    </div>

  </div>
</section>


</BaseLayout>

<!-- JavaScript para el carrusel -->
<script is:inline>
  let currentSlide = 0;
  const slides = document.querySelectorAll('.carousel-slide');
  const indicators = document.querySelectorAll('.carousel-indicator');
  const totalSlides = slides.length;

  function updateCarousel() {
    const track = document.getElementById('carouselTrack');
    if (!track) return;
    
    const translateX = -currentSlide * 100;
    track.style.transform = `translateX(${translateX}%)`;
    
    // Actualizar indicadores
    indicators.forEach((indicator, index) => {
      if (index === currentSlide) {
        indicator.classList.add('active');
        indicator.classList.remove('bg-gray-300');
        indicator.classList.add('bg-blue-600');
      } else {
        indicator.classList.remove('active');
        indicator.classList.remove('bg-blue-600');
        indicator.classList.add('bg-gray-300');
      }
    });
  }

  function nextSlide() {
    currentSlide = (currentSlide + 1) % totalSlides;
    updateCarousel();
  }

  function prevSlide() {
    currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
    updateCarousel();
  }

  function goToSlide(slideIndex) {
    currentSlide = slideIndex;
    updateCarousel();
  }

  // Auto-play del carrusel
  let autoPlay = setInterval(nextSlide, 5000);

  // Pausar auto-play cuando el usuario interactúa
  const carouselContainer = document.querySelector('.carousel-container');
  if (carouselContainer) {
    carouselContainer.addEventListener('mouseenter', () => {
      clearInterval(autoPlay);
    });

    // Reanudar auto-play cuando el mouse sale
    carouselContainer.addEventListener('mouseleave', () => {
      autoPlay = setInterval(nextSlide, 5000);
    });

    // Soporte para gestos táctiles (móvil)
    let startX = 0;
    let endX = 0;

    carouselContainer.addEventListener('touchstart', (e) => {
      if (e.touches && e.touches.length > 0) {
        startX = e.touches[0].clientX;
      }
    });

    carouselContainer.addEventListener('touchend', (e) => {
      if (e.changedTouches && e.changedTouches.length > 0) {
        endX = e.changedTouches[0].clientX;
        const difference = startX - endX;
        
        if (Math.abs(difference) > 50) { // Mínimo swipe de 50px
          if (difference > 0) {
            nextSlide();
          } else {
            prevSlide();
          }
        }
      }
    });
  }

  // Soporte para navegación con teclado
  document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowLeft') {
      prevSlide();
    } else if (e.key === 'ArrowRight') {
      nextSlide();
    }
  });

  // Hacer funciones globales para los onclick
  window.nextSlide = nextSlide;
  window.prevSlide = prevSlide;
  window.goToSlide = goToSlide;
</script>